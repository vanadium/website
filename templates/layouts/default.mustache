<!DOCTYPE html>
<html>
  {{> partials/head }}
  <body>
    {{> partials/header }}
    {{> partials/sidebar }}
    <main>
      <div class="content">
        <h1 class="title">
          {{ page.title }}
        </h1>

        {{# page.toc }}
        <div class="toc"></div>
        {{/ page.toc }}

        {{{ yield }}}
      </div>
    </main>
  </body>
</html>
